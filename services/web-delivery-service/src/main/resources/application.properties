# Logging - for demonstration purposes logs below are set to TRACE level.
# "Change Me" on production codebase!

example.resources.directory=/tmp/streamx

# dev mode (`quarkus dev` command) in Quarkus uses different tenant and namespace than `streamx run` uses
%dev.mp.messaging.incoming.pages.topic=persistent://public/default/pages
%dev.mp.messaging.incoming.web-resources.topic=persistent://public/default/web-resources

%dev.example.resources.directory=./target/repository/resources
